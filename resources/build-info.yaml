- name: build
  source: "."
  image:
    name: maven:3-alpine
    docker_run_params: '-v $HOME/.m2:/root/.m2'
  commands:
    - "echo mvn build"
    - "mkdir targets"
    - "echo testing-creating-jar > targets/1.0-snapshot.jar"
  artifacts:
    - targets/*.jar
